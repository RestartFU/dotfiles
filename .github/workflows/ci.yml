name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Check
        run: ./tools/odinw check
      - name: Test
        run: ./tools/odinw test -define:ODIN_TEST_FANCY=false

