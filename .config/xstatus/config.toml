refresh_delay: 500
seperator: "|"
commands: [ 
	"🔆$(unixtool brightness_percent)%",
	"🔋$(cat /sys/class/power_supply/BAT1/capacity)%",
        "$(date +%a) $(date +%b) $(date +%d)", 
        "$(date +%r)",
    ]
